set imap_user = '<user>'
set imap_pass = ''

set smtp_url = 'smtps://<user>gmail.com'
set smtp_pass = '' 
set folder = imaps://imap.gmail.com/
set spoolfile = +INBOX
set record = "+[Gmail]/Sent Mail"
set postponed = "+[Gmail]/Drafts"
